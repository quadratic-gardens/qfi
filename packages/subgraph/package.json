{
  "name": "@quadratic-funding/subgraph",
  "version": "0.0.1",
  "description": "quadratic-funding subgraph",
  "main": "index.js",
  "author": "",
  "license": "GPL-3.0",
  "private": false,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service",
    "deploy": "yarn codegen && graph deploy --product hosted-service"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.24.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "@types/node": "^17.0.15",
    "typescript": "^4.5.5"
  }
}
